{% extends 'base.html.twig' %}

{% block body %}


{{ form_start(form) }}

Prestito per l'utente {{utente.nome}} {{utente.cognome}}

<table>
  <tr>
    <td> Protocollo: </td>
    <td> {{ form_widget(attribute(form, "protocollo"), { 'attr': {'size': '60'} }) }} </td>  </tr>
  <tr>
    <td> Titolo1: </td>
    <td>{{ form_widget(attribute(form, "titolo1"), { 'attr': {'size': '60'} }) }} </td>
    <!-- 'value': "", -->
  </tr>
  <tr>
    <td> Titolo2: </td>
    <td>{{ form_widget(attribute(form, "titolo2"), { 'attr': {'size': '60'} }) }} </td>
  </tr>
  <tr>
    <td> Collocazione: </td>
    <td>{{ form_widget(attribute(form, "collocazione"), { 'attr': {'size': '60'} }) }} </td>
    <td> <b>{{ form_errors(form.collocazione) }}</b> </td>
  </tr>
  <tr>
    <td> Note: </td>
    <td>{{ form_widget(attribute(form, "note"), { 'attr': {'size': '60'} }) }} </td>
  </tr>

</table>

<input type="submit" value="SALVA" class='button' style='width: 23%; font-size: 20px;'/>

{{ form_end(form) }}

{% endblock %}
